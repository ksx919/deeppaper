spring:
  mail:
    host: smtp.qq.com
    port: 587
    username: 1983262652@qq.com
    password: qaaczftolepvcagh
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  data:
    redis:
      database: 0
      host: 45.197.150.21
      port: 6379
      password: Moyu3renxiaozu
      timeout: 5000
      jedis:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://45.197.150.21:3306/deeppaper?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&&rewriteBatchedStatements=true
    username: root
    password: Moyu3renxiaozu
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1 FROM DUAL
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
# springdoc-openapi项目配置
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.fanxin.controller
# knife4j的增强配置，不需要增强可以不配
knife4j:
  enable: true
  setting:
    language: zh_cn

minio:
  url: http://45.197.150.21:9000
  access-key: admin
  secret-key: Moyu3renxiaozu
  bucket: deeppaper
