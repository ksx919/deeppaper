<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fanxin.mapper.UserMapper">
    <select id="getUserByEmail" resultType="com.fanxin.entity.User">
        SELECT
            id as id,
            nickname as nickname,
            email as email,
            password as password,
            avatar as avatar,
            created_at as createdAt,
            last_login_time as lastLoginTime
        FROM user
        WHERE email = #{email}
    </select>

    <insert id="addUser">
        INSERT INTO user (nickname,email,password,created_at)
        VALUES (#{nickname},#{email},#{password},NOW())
    </insert>

    <update id="updateLoginTime">
        UPDATE user
        SET last_login_time = NOW()
        WHERE email = #{email}
    </update>

    <update id="retrieve">
        UPDATE user
        SET password = #{md5String}
        WHERE email = #{email}
    </update>
</mapper>